{
  "name": "holons-explorer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive explorer for the holons framework: live temporal and structural emergence defects on Game of Life and Lenia.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "verify": "rolldown verify.ts --format esm -o node_modules/.cache/verify.mjs && node node_modules/.cache/verify.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.0.0"
  }
}
